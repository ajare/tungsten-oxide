# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node
node_modules/
dist/
npm-debug.log*

# C++ build output (CMake/Ninja, or an IDE auto-configuring src/core/). The
# vendored nlohmann header and sources are tracked; build trees are not.
build/
src/core/build/
src/applib/build/
src/launcher/build/
src/tungsten-monoxide/build/
ext/willpower/build/
# Per-library Visual Studio build trees (including .vs/IPCH caches).
ext/willpower/**/build/

# ImGui writes its window-layout settings file to the process's working
# directory at runtime (track_editor doesn't override io.IniFilename yet).
imgui.ini
